apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{.Name}}-pdb
  namespace: {{.Namespace}}
  labels:
    app: {{.Name}}
    managed-by: deploy-api
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: {{.Name}}
